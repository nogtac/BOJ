#include <iostream>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0)->ios::sync_with_stdio(false);
	
	int n; cin >> n;

	for(int i = 1; i <=n; i++)
		cout << i << "\n";
	return 0;
}